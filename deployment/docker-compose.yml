services:
  app:
    image: registry.flarewebs.com/fiifidev/blog_test
    restart: always
    ports:
      - "${APP_PORT}:80"
    env_file:
      - .env
    networks:
      - postgres-network

networks:
  postgres-network:
    external: true
    name: 'postgres-network'